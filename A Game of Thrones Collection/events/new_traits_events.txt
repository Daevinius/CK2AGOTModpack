namespace = new_traits_events

character_event = {
	id = new_traits_events.1

	is_triggered_only = yes
	hide_window = yes

	only_men = yes

	immediate = {
		remove_trait = wide_hipped
		remove_trait = narrow_hipped
	}
}

character_event = {
	id = new_traits_events.2

	is_triggered_only = yes
	hide_window = yes

	only_women = yes

	immediate = {
		remove_trait = micro
		remove_trait = well_endowed
	}
}